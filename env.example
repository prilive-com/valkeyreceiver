# Valkey/Redis Connection Settings
VALKEY_RECEIVER_ADDRESS=10.1.0.4:30379
VALKEY_RECEIVER_USERNAME=
VALKEY_RECEIVER_PASSWORD=7Xwdz01BYEu6p74sNRHf8He2
VALKEY_RECEIVER_DATABASE=0

# Connection Timeouts
VALKEY_RECEIVER_DIAL_TIMEOUT=5s
VALKEY_RECEIVER_READ_TIMEOUT=3s
VALKEY_RECEIVER_WRITE_TIMEOUT=3s

# Consumer Settings
VALKEY_RECEIVER_DEFAULT_QUEUES=user-registrations,raw-messages,generic-queue
VALKEY_RECEIVER_CONSUMER_GROUP=demo-consumer-group
VALKEY_RECEIVER_BLOCKING_TIMEOUT=1s
VALKEY_RECEIVER_MAX_CONCURRENT_MESSAGES=10
VALKEY_RECEIVER_PROCESSING_TIMEOUT=30s

# Connection Pool Settings (Production Optimized)
VALKEY_RECEIVER_POOL_SIZE=20
VALKEY_RECEIVER_MIN_IDLE_CONNS=5
VALKEY_RECEIVER_MAX_IDLE_TIME=10m
VALKEY_RECEIVER_CONN_MAX_LIFETIME=1h

# Retry Settings
VALKEY_RECEIVER_MAX_RETRIES=3
VALKEY_RECEIVER_RETRY_DELAY=1s
VALKEY_RECEIVER_RETRY_BACKOFF=2.0

# Circuit Breaker Settings (Fault Tolerance)
VALKEY_RECEIVER_BREAKER_MAX_REQUESTS=10
VALKEY_RECEIVER_BREAKER_INTERVAL=2m
VALKEY_RECEIVER_BREAKER_TIMEOUT=60s

# Rate Limiting (Performance Control)
VALKEY_RECEIVER_RATE_LIMIT_REQUESTS=1000
VALKEY_RECEIVER_RATE_LIMIT_BURST=2000

# Dead Letter Queue
VALKEY_RECEIVER_DEAD_LETTER_QUEUE=failed-messages

# Logging
VALKEY_RECEIVER_LOG_LEVEL=INFO

# TLS Configuration (if needed for production)
# VALKEY_RECEIVER_TLS_ENABLED=true
# VALKEY_RECEIVER_TLS_CERT_FILE=/path/to/cert.pem
# VALKEY_RECEIVER_TLS_KEY_FILE=/path/to/key.pem
# VALKEY_RECEIVER_TLS_CA_FILE=/path/to/ca.pem
# VALKEY_RECEIVER_TLS_SKIP_VERIFY=false